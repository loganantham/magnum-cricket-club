#!/bin/sh
# Wrapper script to run gradlew without JAVA_TOOL_OPTIONS SSL issues
# This script unsets JAVA_TOOL_OPTIONS and lets gradlew handle SSL configuration

# Unset JAVA_TOOL_OPTIONS to prevent conflicts
unset JAVA_TOOL_OPTIONS

# Run gradlew with all arguments
exec "$(dirname "$0")/gradlew" "$@"
